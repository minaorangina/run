var React = require('react');
var ReactDOM = require('react-dom');
var AppContainer = require('./app-container.jsx');

ReactDOM.render(

    <AppContainer />,
    document.getElementsByClassName("container")[0]
);
