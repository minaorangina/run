import React from 'react';

const Arrivals = ({ train, dlr, bus }) => {

    return (
        <div>
            train
            dlr
            bus
        </div>
    );
};

export default Arrivals;
